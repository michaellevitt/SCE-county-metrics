# Python dependencies for the SCE US-County pipeline.
#
# Version bounds: lower = minimum supported; upper = the next MAJOR above the
# highest TESTED release, so patch/minor updates are allowed but an untested
# next-major cannot be silently pulled in. Verified end-to-end on two configs:
#   * Python 3.13 (developer):  numpy 2.4.2, pandas 3.0.0, scipy 1.17.0,
#     scikit-learn 1.8.0, matplotlib 3.10.8, openpyxl 3.1.5, requests 2.32.5,
#     sentence-transformers 5.4.0, torch 2.11.0, umap-learn 0.5.12, pyreadstat 1.3.3
#   * Python 3.11.15 (independent reproduction 2026-07-02):  numpy 2.4.6,
#     pandas 3.0.3, scipy 1.17.1, scikit-learn 1.9.0, torch 2.12.1
#
# The system /usr/bin/python3 (3.9) on macOS may ship a broken numpy -- use a
# clean Python 3.11+ environment:
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt

# ---- core: needed by BOTH pipelines (tables + figures + CC) ----
numpy>=2.0,<3
pandas>=2.0,<4
scipy>=1.11,<2
scikit-learn>=1.3,<2
matplotlib>=3.8,<4
openpyxl>=3.1,<4
requests>=2.31,<3            # generate_cluster_labels.py -> Claude API (needs ANTHROPIC_API_KEY)

# ---- from-raw clustering only (semantic_cluster_metrics_v2.py) ----
# Heavy; NOT needed for run_standard_k120_w1.0.sh, which reuses the
# pre-built clustering in ward_sem_clean2_k120/.
sentence-transformers>=2.2,<6
torch>=2.0,<3
umap-learn>=0.5,<0.6

# ---- AHRF conversion only (code/AHRF_SAS7BDAT_to_CSV...py) ----
# Needed only to convert a HRSA AHRF .sas7bdat download to CSV (see README §7).
pyreadstat>=1.2,<2
