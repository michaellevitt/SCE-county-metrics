# Git LFS — large files tracked via LFS.  Run `git lfs install` once first.
#
# NOTE: data/raw/AHRF2020.fips.csv is NOT in this repo — it is under a HRSA
# Data Use Agreement and cannot be redistributed (see README §"AHRF").
#
# Committed file over the 50 MB warning threshold:
full_w1.0/full_cc_ase0_p=1.0_0.csv  filter=lfs diff=lfs merge=lfs -text
embeddings_mpnet_2745.npy           filter=lfs diff=lfs merge=lfs -text

# Large files GENERATED by a full from-raw run (gitignored by default, but
# kept here so they go to LFS if you ever choose to commit a completed run):
ward_xde_2745/cc_sub.npy                                    filter=lfs diff=lfs merge=lfs -text
full_cc_ase0_p=0.0_25.1.csv                                 filter=lfs diff=lfs merge=lfs -text
BEN_MERGED_MEASURES_imputed_20s_v1.31.GG.Add2024.NEW.csv    filter=lfs diff=lfs merge=lfs -text
BEN_MERGED_MEASURES_imputed_20s_v1.31.GG.Add2024.NORMED.csv filter=lfs diff=lfs merge=lfs -text
